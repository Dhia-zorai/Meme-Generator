# Chef-Claude

A small React project I made to practice React and remember how everything works.

It uses components, state, props, and an API to generate recipes from ingredients.

## Run it

```bash
npm install
npm run dev
```
